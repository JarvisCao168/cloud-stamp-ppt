[pytest]
# 异步测试统一走 pytest-asyncio（asyncio_mode=auto 由 anyio 插件提供时的默认行为）
# test_api.py 为直连 8001 后端的冒烟脚本（async test_* 命名），pytest 收集时必须能跑 async。
asyncio_mode = auto
testpaths =
    backend/tests
    backend/test_api.py
